interface:
  display_name: "Deck Forge"
  short_description: "Build, edit, compare, and verify PPTX or PDF decks."
  default_prompt: "Use $deck-forge to build or minimally edit this deck, preserve the requested structure, and verify every page."
