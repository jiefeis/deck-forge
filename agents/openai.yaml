interface:
  display_name: "Deck Forge"
  short_description: "Generate PDF decks; edit, extend, or audit existing PPTX."
  default_prompt: "Use $deck-forge to build, minimally edit, compare, or audit this deck, preserve the requested structure, and verify every page."
